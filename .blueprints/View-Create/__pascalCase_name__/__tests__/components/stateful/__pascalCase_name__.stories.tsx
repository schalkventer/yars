/* eslint-disable react/jsx-props-no-spreading */

import React, { FC } from 'react';

import { mocks } from '../..';
import { {{pascalCase name}} } from '../../..';

export default { title: 'views/{{pascalCase name}}/stateful/{{pascalCase name}}' };

export const Default: FC = () => <{{pascalCase name}} {...fixtures.PROPS} />;
