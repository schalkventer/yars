import { {{pascalCase name}} } from './{{pascalCase name}}.connector';
import * as types from './{{pascalCase name}}.connector.types';

export { {{pascalCase name}}, types };
export default {{pascalCase name}};
